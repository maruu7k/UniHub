<nav class="navbar navbar-expand-lg navbar-light bg-light py-3">
  <div class="container"><a href="<%= root_path %>" class="navbar-brand d-flex align-items-center"> <i class="fa fa-graduation-cap fa-lg text-primary mr-2"></i><strong>UniHub</strong></a>
    <button type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler"><span class="navbar-toggler-icon"></span></button>
    <div id="navbarSupportedContent" class="collapse navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item active"><a href="<%= root_path %>" class="nav-link font-italic"> Home </a></li>
        <li class="nav-item active"><a href="<%= about_path %>" class="nav-link font-italic"> About </a></li>
        <li class="nav-item active"><a href="<%= contact_path %>" class="nav-link font-italic"> Contact </a></li>
        <% if current_user && !current_user.unihub_applications.nil? %>
          <li class="nav-item active"><a href="<%= my_application_path %>" class="nav-link font-italic"> My Applications</a></li>
        <% end %>
      </ul>

      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item active"><a href="<%= edit_user_registration_path %>" class="nav-link font-italic"> <%= current_user.name.present? ? current_user.name : current_user.email %> </a></li>
          <li class="nav-item active"><a href="<%= destroy_user_session_path %>" class="nav-link font-italic"> Logout </a></li>
        <% else %>
          <li class="nav-item active"><a href="<%= new_user_registration_path %>" class="nav-link font-italic"> Register </a></li>
          <li class="nav-item active"><a href="<%= new_user_session_path %>" class="nav-link font-italic"> Login </a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>